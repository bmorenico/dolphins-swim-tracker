// next.config.js
// Basic Next.js configuration for the Dolphins Swim Tracker.
// reactStrictMode helps catch bugs early during development.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
