// postcss.config.js
// Required helper that lets Tailwind CSS process styles during the build.
// Set once and forget — no need to edit this again.

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
